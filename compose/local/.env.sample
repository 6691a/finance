# Copy to compose/local/.env. Read by docker compose when starting the local
# stack; consumed by the Grafana `finance` datasource provisioning file.
# Quote the password with single quotes so compose does not expand `$`.
FINANCE_DB_URL=
FINANCE_DB_NAME=
FINANCE_DB_USER=
FINANCE_DB_PASSWORD=
