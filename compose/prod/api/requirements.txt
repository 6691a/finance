# 시장 추론 조회 API 운영 의존성. 하한은 루트 pyproject.toml과 맞춘다.
# compose/local/api/requirements.txt와 같은 내용이다 — 함께 고친다(테스트가 대조).
sqlalchemy[asyncio]>=2.0.51
asyncpg>=0.31.0
pydantic>=2.11
pydantic-settings>=2
pyyaml>=6
# apps.core.config가 apps.core.redis(RedisConfig)를 import해서 필요하다. 연결은 안 한다.
redis>=8.1.0
# 프로세스를 죽이는 예외를 Sentry로 보낸다. config.yaml의 sentry_dsn이 비면 비활성.
sentry-sdk>=2
fastapi>=0.136.3
# `uvicorn[standard]`가 아니다. uvloop·httptools·watchfiles 등 다섯을 더 끌고 오는데
# 사설망에서 초당 한 자릿수 요청을 받는 읽기 API에 잴 수 있는 이득이 없다.
uvicorn>=0.52.1
# `run_date`가 KST 세션 날짜라 zoneinfo("Asia/Seoul")를 쓴다. slim 이미지에 시스템
# tzdata가 없을 때의 순수 파이썬 폴백이다.
tzdata
