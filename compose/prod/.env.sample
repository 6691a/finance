# kis-realtime 운영 환경변수. `cp .env.sample .env` 후 채운다. .env는 gitignore 대상이다.
# KIS 키·DB·Sentry는 여기가 아니라 config.yaml에 있다(FastAPI와 공유).

# 저장소 clone 안에서 실행하므로 마운트 경로는 compose 기본값을 쓴다.
# clone 밖에서 돌릴 때만 CODE_DIR(저장소 루트)와 CONFIG_FILE(config.yaml 경로)을 넣는다.

# NXT 체결 구독. 한국장 브리핑이 15:30 이후 NXT 애프터마켓 값을 이 수집에 기대므로
# 운영은 true다(2026-08-19 브리핑 NXT 확장).
KIS_ENABLE_NXT_WEBSOCKET=true

# 분 경계 뒤 몇 초를 기다렸다가 직전 분을 flush할지.
WS_FINALIZATION_DELAY_SECONDS=3

# 잠정 봉을 쓸 config.yaml의 DB alias. read_only: false여야 한다.
REALTIME_DB_ALIAS=default
