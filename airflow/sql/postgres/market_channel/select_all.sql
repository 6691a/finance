-- 프롬프트에 후보로 실을 전달 경로 전부.
--
-- **경로는 수렴한다.** 8주 프로토타입에서 여덟 개에서 멈췄고 둘째 주 이후 여섯 주 연속
-- 새 이름이 0이었다. 그래서 사건과 달리 날짜로 안 좁히고 전부 준다 — 목록이 작을수록
-- 모델이 기존 것을 고르기 쉽다.
--
-- 목록이 프롬프트에 못 실을 만큼 커지면 그때 좁히는 규칙을 만든다. 그 신호는 여기가 아니라
-- 주당 새 이름 수를 재는 관측이 준다(설계 §6 계측 후보).
--
-- 정의의 원본은 `apps/models/analysis/causal.py`의 `MarketChannel`이다.
SELECT id, name
FROM market_channel
ORDER BY id
